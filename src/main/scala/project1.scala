import org.apache.log4j.{Level, Logger}
import org.apache.spark.sql.SparkSession


object project1 {
  def main(args: Array[String]): Unit = {
    // call class to create spark session
    sparkSession

  }
}
